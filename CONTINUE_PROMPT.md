# 下一会话接续提示词（复制到新对话）

将下面 **「【复制区】」** 内的全部内容复制到 Cursor 新对话中发送即可。

---

## 【复制区】

我是 **AI-Agent-PM** 仓库的维护者。工作区路径：`AI-Agent-PM`。

**已完成（需求阶段）**

- 新版本需求全部在 **`PMP_Req_V2/`**，与旧版 **`PMP_Doc/`** 隔离。
- **分册已齐且已定稿（M01～M12）**，入口与索引在 **`PMP_Req_V2/REQ-MASTER_完整需求总文档.md`**。
- 模块概览：M01 立项；M02/M02B/M02C/M02D 需求与技术文档、接口、库表；M03 迭代·Story·Task；M04 Task/看板；M05 人力池；M06 CR；M07 提测/缺陷/联调；M08 项目内 Dashboard；M09 **Vue3 + Element Plus SPA**、侧边栏、按角色分权限的首页/工作台；M10 收尾复盘与知识索引；M11 全站 AI 交互规范；M12 通用规范与发版冒烟验收。
- 产品形态：**管理系统为主**，AI **按需抽屉**；业务数据写入需用户 **显式应用/保存**，文档类支持 **一键撤回 AI 修改并清上下文**（见 M11）。

**接下来请你按顺序协助我**

1. **补充 REQ-MASTER 里的「总述」**  
   - 打开并编辑 **`PMP_Req_V2/REQ-MASTER_完整需求总文档.md`** 中 §3 占位块：补写 **产品愿景与目标用户、范围（含/不含）、全局交互原则（可与 M11 对齐摘录）、术语表与状态机总览（简表即可）、全局非功能与验收口径（可与 M12 对齐摘录）**。  
   - 写完后更新该文档版本号与更新说明；勾选或删除已完成的占位项。

2. **技术选型**  
   - 在 **`PMP_Req_V2/`** 下新增独立技术说明（建议文件名如 **`TECH-001_技术选型与架构说明_v0.1.md`**，或我指定的目录），明确：**前端 Vue3 + Element Plus** 已定；需讨论并写清 **后端语言/框架、数据库、鉴权、API 风格、AI 调用方式（后端代理还是前端直连）、部署形态** 等，并给出 **选型理由与 V1 边界**。

3. **系统架构**  
   - 在同一文档或单独 **`ARCH-OVERVIEW_v0.1.md`** 中给出：**前后端分离结构、核心域模块与 REQ-M01～M12 的映射、多租户/项目隔离、权限模型（对接 M09/M01）、文档与版本存储策略、与 AI 服务的边界**；可用 **文字 + Mermaid 架构图**。

4. **仓库文件结构**  
   - 给出 **`AI-Agent-PM` 单仓或多包（monorepo）的推荐目录树**（例如 `apps/web`、`apps/api`、`packages/shared`），说明各目录职责、与上述模块的对应关系，以及后续可落地的 **迭代 0 脚手架顺序**。

**工作方式**

- 先通读 **`REQ-MASTER`** 与 **`PMP_Req_V2/README.md`**，改动以 Markdown 落盘为准；与我确认后再大改结构。  
- 回答与文档使用 **简体中文**。

---

## 【复制区结束】
